apiVersion: v1
kind: Pod
metadata:
  name: workload-a-test-pod
  namespace: nginx
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
